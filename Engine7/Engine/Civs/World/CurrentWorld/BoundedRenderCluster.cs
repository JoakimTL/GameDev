//using Engine;

////TODO: Have tiles be outside the ECS systems. Incorporate them into the ECS system (and thus rendering) as tile collection components: Lists of tiles. These lists are gained from the octree in the globe class.

//namespace Civs.World.CurrentWorld;

//public sealed class BoundedRenderCluster( uint id, AABB<Vector3<float>> bounds, IReadOnlyList<uint> faces, IReadOnlyList<uint> edges ) {
//	public uint Id { get; } = id;
//	public AABB<Vector3<float>> Bounds { get; } = bounds;
//	public IReadOnlyList<uint> FaceIds { get; } = faces;
//	public IReadOnlyList<uint> EdgeIds { get; } = edges;
//}
